1. Основы

Javascript в браузерах - это язык сценариев для придания интерактивности веб-страницам.

1.0 Firebug / Chrome Dev Tools / IDE

1.1 Структура HTML5 страницы

1.2.0 Код и коментарии

1.2.1 Переменные / Область видимости window
    Переменная состоит из имени и выделенной области памяти, которая ему соответствует.
    var a = 'Hello World';

    a = 'New Message';
    ----------
    Зарезвированные слова

    var typeof a;// = error;
    ----------
    Code style при обьявление переменных - CamelCase
    var Moe_SOOBSHeniE; // bad
    var myMessage; // good

1.3 Типы данных
1.3.1 Примитивные типы:
    Число: var n = 1;
           var n = -2.1;

    Специальные числовые значения
    alert(1 / 0); // Infinity
    alert("not a number" * 2 ); // NaN

    Строка: var str = 'Hello World';

    Логический: var status = true;

    Null: var a = null;

    Undefined: var a;

1.3.2 Обьекты

var date = new Date();

var obj = {
    key : 'value'
};

var obj = new Object();

var arr = [1,2,3]; // push // pop

function sum(a, b) {
    return a + b;
}

var result = sum(1,3);

1.4. Основные операторы
    Арифметические
    * / + - += *=

    Инкремент,Декремент
    i++ ++i --i i--

    операторы сравнения
    var a = 2 > 1; // true;

    if (2 > 1) {
        alert('yes');
    } else {
        alert('no');
    }


1.5 циклы

    for in // for

1.6 ООП ( если успеем )

1.6 ДЗ - написать обьект, свойства которого будут функции которые:
    а. возвращает квадрат числа
    б. возвращает максимальное значение из массива целых чисел
    в. возвращает находится ли елемент x, в массиве y.
    г. функцию которая возвращает срез массива от индекса x, до индекса y.
    д. функцию которая возвращает среднее арифметическое от двух чисел.

